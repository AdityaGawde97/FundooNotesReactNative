import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    row: {
        flexDirection: 'row',
        paddingVertical: 15,
        paddingLeft: 25
    },
})

export default styles